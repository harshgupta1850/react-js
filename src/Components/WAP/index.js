import React, { Fragment } from "react";
import { duplicate } from "./DuplicateCharacters";
import { sortCities } from "./ParticularSorting";

const WAP = () => {
  // console.log(duplicate());
  console.log(sortCities)
  return (
    <Fragment>
      <h1>JSclear...............</h1>
    </Fragment>
  );
};

export default WAP;
