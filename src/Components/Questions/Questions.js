// Questions
// Functional Programming questions
// - take an array of numbers and find sum of all odd numbers
// - take an array of numbers and find sum of numbers at odd indice
// - take an array of numbers and find biggest number in the array
// - take an array of numbers and find return an array with only even numbers doubled 
// - take an array of numbers and return an object like {odd:<no.of odd numbers in array>,even:<no.of even numbers in array>}
// - take an array of words and return an object such that {[length of word]:<no. of words with that length>}
// - take an array of words and return an array of strings with no vowels
// - return object -> {<word> : <word.length>}
// - make the map function from reduce
// - make the filter function from reduce
// - Take an array of array and flatten them into a single array using only reduce once










